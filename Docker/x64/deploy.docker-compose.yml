version: '3.4'
services:
  image_rotate:
    container_name: prod_image_rotate
    image: dock101/image_rotate:x64
    restart: "always"
    volumes:
      - ./:/usr/src/app/build
    network_mode: host