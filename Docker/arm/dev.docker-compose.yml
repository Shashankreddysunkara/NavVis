version: '3.4'
services:
  image_rotate:
    image: image_rotate.dev:arm
    restart: "always"
    volumes:
      - .persistent_volume/:/usr/src/app/build
    stdin_open: true
    tty: true
    network_mode: host